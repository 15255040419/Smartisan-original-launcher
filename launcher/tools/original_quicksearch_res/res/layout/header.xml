<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">
    <TextView
        android:id="@+id/sortkey"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        style="@style/StickyListHeaderStyle" />
    <ImageView
        android:id="@+id/clear_history"
        android:layout_width="wrap_content"
        android:layout_height="30.0dp"
        android:layout_alignParentRight="true"
        android:layout_centerVertical="true"
        android:contentDescription="@string/accessibility_clear_history_text"
        android:paddingLeft="9.0dp"
        android:paddingRight="9.0dp"
        android:scaleType="centerInside"
        android:src="@drawable/history_clear_btn"
        android:visibility="gone" />
</RelativeLayout>
