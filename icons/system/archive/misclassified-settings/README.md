# Historical misclassified system artwork

These PNGs previously appeared as `com.android.settings_*` candidates but do not
represent the Android Settings application. They are retained only as source
history and are intentionally excluded from the online runtime library.

Do not add them back to `icons/variants.json` unless a verified system category
and matching rule are added first.
